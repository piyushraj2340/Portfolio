# Public site origin used for metadata, sitemap, robots, and JSON-LD
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Contact API / email (server-only)
MONGODB_URI=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=
